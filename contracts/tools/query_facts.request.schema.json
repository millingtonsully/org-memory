{
  "$comment": "MCP query_facts argument schema (org-memory HTTP adapter).",
  "type": "object",
  "required": ["subject_type", "subject_id"],
  "properties": {
    "subject_type": {"type": "string", "minLength": 1},
    "subject_id": {"type": "string", "minLength": 1},
    "predicate": {
      "type": "string",
      "description": "Registry predicate key; omit to return all matching predicates for the subject."
    },
    "as_of": {
      "type": "string",
      "format": "date-time",
      "description": "World-time point for validity-window lookup. Omit for currently active open-interval claims."
    },
    "limit": {"type": "integer", "minimum": 1, "maximum": 200, "default": 50}
  },
  "additionalProperties": false
}
