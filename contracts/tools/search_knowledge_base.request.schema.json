{
  "$comment": "MCP search_knowledge_base argument schema (org-memory HTTP adapter).",
  "type": "object",
  "required": ["query"],
  "properties": {
    "query": {"type": "string", "minLength": 1},
    "source_type": {
      "type": "string",
      "description": "Opaque object-kind label (free string; same as ChangeEnvelope.source_type)."
    },
    "source_system": {
      "type": "string",
      "description": "Opaque connector/system id (free string; same as ChangeEnvelope.source_system)."
    },
    "author": {"type": "string"},
    "date_from": {"type": "string", "format": "date"},
    "date_to": {"type": "string", "format": "date"},
    "updated_from": {"type": "string", "format": "date"},
    "updated_to": {"type": "string", "format": "date"},
    "doc_id": {"type": "string"},
    "limit": {"type": "integer", "minimum": 1, "maximum": 200, "default": 50},
    "half_life_days": {
      "type": "number",
      "minimum": 1,
      "maximum": 365,
      "default": 90
    },
    "min_decay": {"type": "number", "minimum": 0, "maximum": 1, "default": 0.3}
  },
  "additionalProperties": false
}
